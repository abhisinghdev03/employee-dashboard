function App() {
  const appName = "Employee Dashboard";
  const version = "0.1.0";

  return (
    <div>
      <h1>{appName}</h1>
      <p>Build tooling: Vite | Runtime: React</p>
      <footer>v{version}</footer>
    </div>
  );
}

export default App;